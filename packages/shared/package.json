{
    "name": "@mutual-hub/shared",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "lint": "npm run typecheck",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "test": "vitest run"
    },
    "dependencies": {
        "@mutual-hub/at-lexicons": "0.1.0",
        "zod": "3.25.76"
    }
}
