{
    "name": "@mutual-hub/shared",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "lint": "biome check src",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "test:moderation-privacy": "tsx --test src/domains/moderation.test.ts src/domains/privacy.test.ts src/index.test.ts",
        "test": "tsx --test src/**/*.test.ts"
    }
}
