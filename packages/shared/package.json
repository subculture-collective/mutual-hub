{
    "name": "@mutual-hub/shared",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "lint": "npm run typecheck",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "test": "vitest run",
        "test:phase7": "vitest run src/moderation.test.ts src/messaging.test.ts src/privacy.test.ts src/firehose.test.ts"
    },
    "dependencies": {
        "@mutual-hub/at-lexicons": "0.1.0",
        "zod": "3.25.76"
    }
}
